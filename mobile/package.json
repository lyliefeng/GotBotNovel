{
  "name": "gotbotnovel-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "GotBotNovel Android client",
  "type": "module",
  "scripts": {
    "copy-web": "node scripts/copy-web.mjs",
    "sync": "npm run copy-web && cap sync android",
    "android:open": "npm run sync && cap open android",
    "android:build": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2",
    "typescript": "~5.9.3"
  }
}
